{
  "extends": ["@commitlint/config-conventional"],
  "rules": {
    "scope-enum": [2, "always", []],
    "subject-case": [0],
    "type-enum": [2, "always", ["feat", "fix", "docs", "refactor", "test", "chore"]]
  }
}
